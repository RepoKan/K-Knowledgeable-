# Model Routing Governance

Canonical rule: `K_KNOWLEDGE_SUPPORTING_MODEL_ROUTING_R1.md` (`KKS-MODEL-ROUTING-R1`).

Keep `mcp_config.json` focused on MCP transport/tool configuration. Resolve model choice from the active product/workspace first and re-verify official OpenAI availability according to the rule's freshness gate.
